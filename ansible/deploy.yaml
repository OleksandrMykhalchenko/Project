---
- hosts: deploy
  become: True
  tasks:
    - name: Download setuptools
      apt:
        name: python-setuptools
        state: present
    - name: Install pip
      apt:
        name: pip
        state: present
    - name: Install docker
      apt:
        name: docker
        state: present
    - name: Ensure docker starts on boot
      shell: systemctl start docker.service
    - name: Ensure docker launches on boot
      shell: systemctl enable docker.service
    - name: Install docker-py
      pip:
        name: docker-py
        state: present
    - name: Start the container
      docker_container:
        name: petclinic
        image: "oleksandrmykhalchenko/petclinic:{{DOCKER_TAG}}"
        state: started
        published_ports:
          - 0.0.0.0:8080:8080
